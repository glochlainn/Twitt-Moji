{% form_theme form 'bootstrap_5_layout.html.twig' %}

{{ form_start(form)}}
<div class="mt-3">
    {{form_widget(form.content, {'attr': {'class': 'form-control shadow-sm p-3 mb-3 rounded'}})}}
</div>
<button class="btn btn-secondary text-light fw-bold">
    {{ button_label|default('ENVOYER') }}<i class="ms-2 far fa-paper-plane"></i>
</button>
{{form_end(form)}}
